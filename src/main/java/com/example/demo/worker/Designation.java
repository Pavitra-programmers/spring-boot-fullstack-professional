package com.example.demo.worker;

/**
 * Job designation of a construction site worker.
 * Stored as string in DB for readability and forward compatibility.
 */
public enum Designation {
    MASON,
    CARPENTER,
    ELECTRICIAN,
    PLUMBER,
    LABORER,
    FOREMAN,
    SUPERVISOR
}
